<div class="modal fade" id="demoInfoModal" tabindex="-1" aria-labelledby="demoModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content shadow">
      <div class="modal-header bg-info text-white">
        <h5 class="modal-title" id="demoModalLabel">👋 ¡Bienvenido a la demo del torneo!</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      <div class="modal-body">
        <p>Selecciona un equipo para seguir el torneo desde su punto de vista. Podrás ver:</p>
        <ul>
          <li>Partidos jugados y próximos</li>
          <li>Clasificación en tiempo real</li>
          <li>Cruces eliminatorios automáticos</li>
        </ul>
        <p class="mt-3 text-warning">
        ⚠️ Estás viendo la versión pública de la demo. Los datos pueden modificarse desde la
        <a href="{{ path('seguimiento_vivo', { slug: torneo.slug }) }}"
            class="link-warning fw-bold"
            target="_blank" rel="noopener noreferrer">
            zona privada de organización
        </a>.
        </p>
        <p class="mt-2">
        ¿Te gustaría gestionar tu propio torneo así?
        <a href="mailto:torneos@apgijon.es" class="link-primary">¡Escríbenos!</a>
        </p>
        <p class="mt-3 text-muted small text-center">
        Este mensaje se muestra automáticamente solo en modo demo. Puedes volver a verlo haciendo clic en <strong>❓ Ayuda demo</strong>.
        </p>        
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">¡Entendido!</button>
      </div>
    </div>
  </div>
</div>

<script>
document.addEventListener("DOMContentLoaded", function () {
    if (!sessionStorage.getItem("demoModalShown")) {
        const modalElement = document.getElementById('demoInfoModal');
        if (modalElement) {
            const modal = new bootstrap.Modal(modalElement);
            modal.show();
            sessionStorage.setItem("demoModalShown", "1");
        }
    }

   
});

  function mostrarDemoInfo() {
      const modalElement = document.getElementById('demoInfoModal');
      if (modalElement) {
          const modal = new bootstrap.Modal(modalElement);
          modal.show();
      }
  } 
</script>
