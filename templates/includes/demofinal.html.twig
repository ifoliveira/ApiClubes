
<div class="modal fade" id="demoFinalModal" tabindex="-1" aria-labelledby="demoFinalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content shadow">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="demoFinalLabel">🏆 Evolución del Cuadro Final</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      <div class="modal-body">
        <p>
          Estás viendo el cuadro final del torneo con los cruces eliminatorios generados automáticamente.
        </p>
        {% if equipoSeleccionado %}
        <p>
          Desde el punto de vista del equipo <strong>{{ equipoSeleccionado.nombre }}</strong>.
        </p>
        {% endif %}

        <ul>
          <li>📅 Consulta fechas, resultados y enfrentamientos</li>
          <li>🔄 Cruces generados según clasificación de grupos</li>
          <li>👁️ Sigue la evolución hasta la final</li>
        </ul>

        <p class="mt-3 text-warning">
          ⚠️ Estás viendo la versión pública de la demo. Los datos pueden modificarse desde la
          <a href="{{ path('seguimiento_vivo', { slug: torneo.slug }) }}"
             class="link-warning fw-bold"
             target="_blank" rel="noopener noreferrer">
            zona privada de organización
          </a>.
        </p>

        <p class="mt-2 text-muted small text-center">
          Este mensaje se muestra solo en modo demo. Puedes volver a verlo desde <strong>❓ Ayuda demo</strong>.
        </p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Entendido</button>
      </div>
    </div>
  </div>
</div>


<script>
document.addEventListener("DOMContentLoaded", function () {
    if (!sessionStorage.getItem("demoFinalModalShown")) {
        const modalElement = document.getElementById('demoFinalModal');
        if (modalElement) {
            const modal = new bootstrap.Modal(modalElement);
            modal.show();
            sessionStorage.setItem("demoFinalModalShown", "1");
        }
    }
});

function mostrarDemoFinalModal() {
    const modalElement = document.getElementById('demoFinalModal');
    if (modalElement) {
        const modal = new bootstrap.Modal(modalElement);
        modal.show();
    }
}
</script>
