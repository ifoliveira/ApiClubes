{% set demoModalType = 'demoadmin' %}

{% extends 'baseadmin.html.twig' %}
{% block stylesheets %}
    <style>
.custom-modal.hidden {
    display: none;
}

.custom-modal {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
}

.custom-modal-backdrop {
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.7);
}

.custom-modal-content {
    position: relative;
    background: #1e293b;
    color: white;
    width: 90%;
    max-width: 420px;
    border-radius: 1rem;
    padding: 1.5rem;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
    z-index: 10000;
}

.custom-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
}

.custom-modal-header h2 {
    margin: 0;
    font-size: 1.2rem;
}

.custom-modal-close {
    background: none;
    border: none;
    color: white;
    font-size: 1.5rem;
    cursor: pointer;
}

.custom-modal-body ul {
    padding-left: 1.2rem;
    margin: 0.8rem 0;
}

.custom-modal-body li {
    margin-bottom: 0.5rem;
}



    </style>
{% endblock %}
{% block title %}Inicio Administración{% endblock %}

{% block content %}
    <h2>¡Bienvenido al panel de control!</h2>
    <p>Selecciona una opción en los botones de arriba para gestionar el torneo.</p>
{% endblock %}